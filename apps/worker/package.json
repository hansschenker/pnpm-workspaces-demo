{
    "name": "@netxpert/worker",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "wrangler dev",
        "build": "wrangler types && tsc -b",
        "deploy": "wrangler deploy"
    },
    "dependencies": {
        "@netxpert/api": "workspace:*",
        "hono": "catalog:"
    },
    "devDependencies": {
        "@types/node": "catalog:",
        "typescript": "catalog:",
        "wrangler": "catalog:"
    }
}
