{
	"name": "hono",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"@jilles/database": "workspace:*",
		"hono": "^4.9.9"
	},
	"devDependencies": {
		"wrangler": "^4.40.2"
	}
}